export const DATE_FORMAT = 'DD/MM/YYYY';

export const enum STATUS {
  DELETED = -1,
  PASSIVE = 0,
  ACTIVE = 1,
  SWAPED = 2,
}

export const enum STATUS_TEXT {
  DELETED = 'Silinmiş',
  PASSIVE = 'Pasif',
  ACTIVE = 'Aktif',
  SWAPED = 'Takaslanmış',
}
